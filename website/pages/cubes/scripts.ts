import "./style.css";

import "../../src/components/navbar";
import "./cube-scene";
import "./cube-block";
import "./cube-grid";

/*
What we need for an editor
- adding different kinds of cubes (cube, pyramid, slop/prism, celinder, sphere )
- scene grid options: height/width/depth, units, sizes
- scene rotations

- drag and drop
- eraser, pointer

*/
